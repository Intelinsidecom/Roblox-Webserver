<Window x:Class="Control_Panel.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Control_Panel"
        mc:Ignorable="d"
        Title="Control Panel" Height="650" Width="900"
        Background="{DynamicResource Background}"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Window.Resources>
    </Window.Resources>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Console" Click="ConsoleMenuItem_Click">
                <MenuItem.Icon>
                    <TextBlock Text="▶" FontSize="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Settings" Click="SettingsMenuItem_Click">
                <MenuItem.Icon>
                    <TextBlock Text="⚙" FontSize="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Service Setup" Click="ServiceSetupMenuItem_Click">
                <MenuItem.Icon>
                    <TextBlock Text="🔧" FontSize="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="About" Click="AboutMenuItem_Click">
                <MenuItem.Icon>
                    <TextBlock Text="ℹ" FontSize="16"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:SettingsOverlay x:Name="GlobalSettingsOverlay" Panel.ZIndex="9999" Grid.ColumnSpan="2"/>
        <local:Sidebar Grid.Column="0"/>
        <Grid Grid.Column="1" Margin="20" x:Name="MainContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentControl x:Name="ViewContainer" Grid.Row="0" Grid.RowSpan="3"/>
            <Border Grid.Row="2" Background="{DynamicResource AccentPrimary}" 
                    Padding="15,10" Margin="0,15,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="StatusTextBlock" Text="Ready" 
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
