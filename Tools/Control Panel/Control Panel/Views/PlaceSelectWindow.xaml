<Window x:Class="Control_Panel.PlaceSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Control_Panel"
        mc:Ignorable="d"
        Title="Place Select" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Background}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="WindowTitleText" Text="Place Select"
                           Style="{StaticResource DashboardTitle}"
                           Foreground="{DynamicResource AccentPrimary}"/>
                <TextBlock x:Name="WindowSubtitleText" Text="Select a place to manage it"
                           Style="{StaticResource DashboardSubtitle}"
                           Foreground="{DynamicResource SubtleText}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Refresh"
                        Style="{StaticResource ModernButton}"
                        Background="{DynamicResource AccentPrimary}"
                        Foreground="White"
                        Width="80" Height="35"
                        Click="RefreshButton_Click"/>
                <Button Content="Edit Game"
                        Style="{StaticResource ModernButton}"
                        Width="100" Height="35"
                        Margin="8,0,0,0"
                        Click="EditGameButton_Click"/>
            </StackPanel>
        </Grid>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False">
            <ItemsControl x:Name="PlaceItemsControl" ClipToBounds="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" ClipToBounds="False" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Panel.ZIndex" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Panel.ZIndex" Value="100"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button x:Name="PlaceCardRoot" Margin="0,0,12,12" Width="150" Height="180" Cursor="Hand"
                                Background="Transparent" BorderThickness="0"
                                Click="PlaceCard_Click" Tag="{Binding PlaceId}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Background="Transparent" ClipToBounds="False">
                                        <Border x:Name="PlaceCardBorder" Width="150" Height="180"
                                                Background="{DynamicResource Background}"
                                                BorderBrush="{DynamicResource BorderColor}"
                                                BorderThickness="1"
                                                CornerRadius="3"
                                                VerticalAlignment="Top">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="120"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <Border Grid.Row="0" Width="138" Height="120" Margin="6,6,6,0"
                                                        CornerRadius="3,3,0,0" ClipToBounds="True"
                                                        HorizontalAlignment="Center">
                                                    <Border.Background>
                                                        <ImageBrush ImageSource="{Binding ThumbnailUrl}" Stretch="Uniform"/>
                                                    </Border.Background>
                                                </Border>
                                                <StackPanel Grid.Row="1" Margin="6,4,6,4">
                                                    <TextBlock Text="{Binding Name}" 
                                                               Foreground="{DynamicResource Foreground}"
                                                               FontSize="13" FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding SecondaryText}" 
                                                               Foreground="{DynamicResource SubtleText}"
                                                               FontSize="11" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <Border x:Name="PlaceCardHoverOverlay"
                                                Background="{DynamicResource Background}"
                                                BorderBrush="{DynamicResource BorderColor}"
                                                BorderThickness="1,0,1,1"
                                                CornerRadius="0,0,3,3"
                                                Width="150"
                                                VerticalAlignment="Top"
                                                Panel.ZIndex="9999">
                                            <Border.RenderTransform>
                                                <TranslateTransform Y="179"/>
                                            </Border.RenderTransform>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, ElementName=PlaceCardRoot}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Margin="8,6,8,8">
                                                <TextBlock FontSize="11" Foreground="{DynamicResource AccentPrimary}"
                                                           FontWeight="SemiBold"
                                                           Text="{Binding PlaceIdLabel}" 
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
