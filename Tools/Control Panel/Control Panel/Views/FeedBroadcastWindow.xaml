<Window x:Class="Control_Panel.Views.FeedBroadcastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Control_Panel.Views"
        mc:Ignorable="d"
        Title="Broadcast Feed Message" Height="260" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Background}"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Broadcast to All Users' Feed"
                   Style="{StaticResource DashboardTitle}"
                   Foreground="{DynamicResource AccentPrimary}"
                   FontSize="18"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Message" Style="{StaticResource LabelText}" Margin="0,0,0,5"/>
            <TextBox Grid.Row="1"
                     x:Name="MessageTextBox"
                     Style="{StaticResource ModernTextBox}"
                     Padding="5,3"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     VerticalContentAlignment="Top"/>
        </Grid>

        <TextBlock Grid.Row="2"
                   x:Name="StatusText"
                   Text=""
                   Style="{StaticResource LabelText}"
                   Margin="0,10,0,10"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Content="Cancel"
                    Style="{StaticResource ModernButton}"
                    Background="{DynamicResource SubtleText}"
                    Foreground="White"
                    Width="80" Height="35"
                    Click="CancelButton_Click"/>
            <Button Grid.Column="3"
                    x:Name="PostButton"
                    Content="Post"
                    Style="{StaticResource ModernButton}"
                    Background="{DynamicResource AccentPrimary}"
                    Foreground="White"
                    Width="80" Height="35"
                    Click="PostButton_Click"/>
        </Grid>
    </Grid>
</Window>
